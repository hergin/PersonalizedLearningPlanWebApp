{
    "name": "learning-plan-web-app-server",
    "version": "0.1.0",
    "private": true,
    "dependencies": {
        "bcrypt": "^5.1.1",
        "cors": "^2.8.5",
        "dotenv": "^16.3.1",
        "express": "^4.18.2",
        "jest": "^29.5.0",
        "nodemon": "^3.0.1",
        "pg": "^8.11.3"
    },
    "scripts": {
        "dev": "nodemon app.js",
        "test": "jest --testPathPattern=tests --detectOpenHandles --coverage"
    },
    "eslintConfig": {
        "extends": [
            "react-app",
            "react-app/jest"
        ]
    },
    "browserslist": {
        "production": [
            ">0.2%",
            "not dead",
            "not op_mini all"
        ],
        "development": [
            "last 1 chrome version",
            "last 1 firefox version",
            "last 1 safari version"
        ]
    },
    "devDependencies": {
        "@babel/preset-env": "^7.23.3",
        "babel-jest": "^29.7.0"
    },
    "jest": {
        "transform": {
            "^.+\\.(js|jsx)$": "babel-jest"
        },
        "verbose": true,
        "testEnvironment": "node",
        "testPathIgnorePatterns": [
            "/node_modules/"
        ]
    },
    "babel": {
        "presets": [
            "@babel/preset-env"
        ]
    }
}
